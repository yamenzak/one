{
  "name": "@quad/one",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "One \u2014 THE deployment. The single worker that answers every door for every product, and serves the Hub. Quad is what it is built on; One is what people open.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@quad/kernel": "workspace:*",
    "@quad/runtime": "workspace:*",
    "@quad/hello": "workspace:*",
    "@quad/kova": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.8.55",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.42.0",
    "@cloudflare/workers-types": "^4.20260702.1"
  }
}
