{
  "name": "@quad/hello",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The smallest complete app on Quad: one collection, one operation, and every cross-cutting declaration wired. It exists to be proved — every stage's exit criterion is asserted against it.",
  "exports": { ".": "./src/index.ts" },
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@quad/kernel": "workspace:*",
    "@quad/runtime": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.8.55",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
