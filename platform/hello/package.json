{
  "name": "@one/hello",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The smallest real app on ONE: a manifest, one collection, three operations. It exists to boot — every stage's exit criterion is asserted against it.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "dependencies": { "@one/kernel": "workspace:*", "@one/runtime": "workspace:*" },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.8.55",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
