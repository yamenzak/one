name: CI

# Pre-merge validation. `deploy.yml` only runs on push to main, which means
# nothing used to be checked BEFORE a merge — a PR could go green on nothing and
# break main on the way in. This runs the same install → build → typecheck →
# test chain as the deploy gate, but touches no Cloudflare credentials and
# deploys nothing.
#
# Adding an app is a `apps.json` ENTRY, not an edit here. Both jobs derive their
# work from the registry, because the last two CI failures on main were a per-app
# step somebody forgot to duplicate: Tessa's SPA build was missing, so its whole
# Miniflare suite reported "no tests" — a PASS-shaped result.
on:
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  # Superseding a PR's own older run is fine here — nothing is deployed, so a
  # cancelled run leaves no partial state.
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    outputs:
      e2e: ${{ steps.registry.outputs.e2e }}
      any: ${{ steps.registry.outputs.any }}
    steps:
      # FULL history: the E2E filter below diffs this PR against its base, and a
      # shallow clone cannot resolve one.
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }

      # Both guards go BEFORE install: a check that needs the workspace
      # installed cannot guard the workspace's own install, and both of these
      # catch failures that are SILENT rather than loud.
      #
      # A workflow with broken YAML does not FAIL — it does not RUN. GitHub
      # lists it by filename, its Run-workflow button never appears, and nothing
      # says why. That is how provision-tessa.yml shipped dead.
      - run: node .github/workflows/workflows-parse.test.mjs
      # An app that is not in the registry is not built and not deployed, and
      # nothing reports it — see the header.
      - run: node scripts/apps-manifest.test.mjs
      # The same filter `deploy.yml` ships with, guarded the same way.
      - run: node scripts/affected.test.mjs

      # No version — read from package.json's packageManager.
      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # ── The single biggest lever on this workflow's runtime ────────────────
      #
      # Measured on this repo, from cold: typecheck 1m17s, tests 4m25s, the SPA
      # builds 24s. Warm, all of it is 0.5s — turbo hashes each task's inputs and
      # replays the outputs. CI threw that cache away on every run and paid the
      # ~6 minutes again.
      #
      # Restoring it is SAFE in a way a build cache usually is not: the key is
      # the content of a task's inputs, so a package that changed misses and
      # re-runs. A stale cache cannot make a broken commit pass — it can only
      # fail to save time.
      #
      # Keyed by SHA so every run writes a fresh entry (GitHub caches are
      # immutable per key, so a fixed key would freeze on the first run), and
      # restored by prefix so it picks up the newest. 540 KB an entry.
      - uses: actions/cache@v4
        with:
          path: .turbo/cache
          key: turbo-${{ runner.os }}-${{ github.sha }}
          restore-keys: turbo-${{ runner.os }}-

      # EVERY SPA, from the registry. Each worker serves its app through an
      # `assets` binding, and the Miniflare integration suites report "no tests"
      # — a PASS-shaped result — when the directory is missing. Turbo cannot
      # infer this: an `assets.directory` is a filesystem path, not a package
      # dependency, so nothing in the graph connects a worker's tests to its
      # app's build. Deriving the list from `apps.json` is what stops it being
      # explicit-and-stale, which is exactly how Tessa's went missing.
      # Through TURBO, not `pnpm --filter`. Same builds, but inside the graph:
      # they hash, they cache, and a run that changed no SPA replays them in
      # milliseconds instead of spending 24 seconds on vite. The list still comes
      # from the registry — `turbo.json` now declares each worker suite's edge to
      # its SPA, but the explicit build is what covers an app whose suite has not
      # been given one yet.
      - name: Build every SPA
        run: |
          set -euo pipefail
          filters=$(node scripts/apps.mjs spas | sed 's/^/--filter=/' | tr '\n' ' ')
          pnpm turbo run build $filters

      # ── The test lane runs SERIALLY, and that is not a style choice ───────
      #
      # `pnpm turbo run typecheck test` ran both lanes at turbo's default
      # concurrency of 10. On a 2-core runner that starts three Workers-pool
      # suites (Kova, Tessa, Scena) alongside several `tsc` processes, and
      # Scena's died 10 ms after `Starting single runtime` having printed
      # NOTHING — no assertion, no stack, no `Shutting down runtimes`, and a
      # `workerd` left orphaned for the runner to reap. That is the shape of a
      # process being killed, not of a test failing.
      #
      # It is not reproducible on a developer machine with 16 GB and 4 cores,
      # even when the three suites are started deliberately at once — which is
      # why it reached main green and failed here. `retry: 1` in each pool
      # config cannot help: the runtime dies before a test exists to retry.
      #
      # So the fix is to stop the contention rather than to raise a timeout.
      # Typecheck still runs fully parallel; the test lane is serial. Turbo
      # caches both, so a warm run replays in milliseconds either way and only a
      # cold run pays — a few minutes for a lane that otherwise fails silently
      # and blocks every deploy behind it.
      #
      # Measured on this repo: the test lane is 2m54s parallel and 6m41s
      # serial from COLD, and 46 ms either way once turbo's cache is warm — and
      # CI restores that cache between runs. So the cost lands on the runs that
      # changed a lot, which are the runs where a silently-killed suite is least
      # affordable.
      - run: pnpm turbo run typecheck
      - run: pnpm turbo run test --concurrency=1

      # ── Which Playwright suites does this PR actually need? ───────────────
      #
      # Unit tests above run WHOLE, every time — they are the safety net and
      # narrowing them is how a cross-package break gets through. E2E is the
      # different case: it is the slow lane (a Chromium download plus a real
      # browser against a real worker, per product), and a PR touching one
      # product has no reason to drive the other's golden paths.
      #
      # Same filter as the deploy, plus one addition: an E2E suite's own files
      # count as a change to it. The deploy filter deliberately ignores a spec
      # edit — it must not mint a Worker version — but it must absolutely be run.
      #
      # Fails open exactly as the deploy does: an unresolvable base runs
      # everything.
      - id: registry
        env:
          BASE: ${{ github.event.pull_request.base.sha }}
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" != "pull_request" ]; then
            m=$(node scripts/apps.mjs e2e-matrix)
          else
            m=$(node scripts/affected.mjs "$BASE" --e2e)
          fi
          echo "e2e=$m" >> "$GITHUB_OUTPUT"
          n=$(printf '%s' "$m" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>process.stdout.write(String(JSON.parse(s).include.length)))")
          echo "any=$([ "$n" -gt 0 ] && echo true || echo false)" >> "$GITHUB_OUTPUT"
          [ "$n" -gt 0 ] || echo "::notice::No E2E suite is affected by this PR."

  # One job per registered Playwright suite — Kova's three golden paths
  # (SPEC §12), Tessa's recall and scan paths — in a real browser against the
  # real worker.
  #
  # Separate jobs on purpose, rather than one appended run: each boots its own
  # worker on its own port (8787 / 8788), and a shared job would make "which
  # product is broken" a question you answer by reading a log. They also need a
  # Chromium download the unit lanes don't.
  #
  # Credential-free and network-free, like `validate`: Playwright boots
  # `wrangler dev --local` (D1/KV/R2 simulated by Miniflare), each suite creates
  # its own tenants and users through the app's own sign-in, and nothing calls
  # Workers AI, Stripe, or an external provider.
  e2e:
    needs: validate
    # An EMPTY include is a workflow ERROR in GitHub's matrix expansion, not an
    # empty job list — so "nothing to run" is expressed here, not by the matrix.
    if: needs.validate.outputs.any == 'true'
    runs-on: ubuntu-latest
    strategy:
      # One product's flake must not hide the other's real failure.
      fail-fast: false
      matrix: ${{ fromJson(needs.validate.outputs.e2e) }}
    name: e2e ${{ matrix.id }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile

      # The BROWSER is cached; the system libraries are not, and cannot be —
      # `--with-deps` is an apt install, which lands outside any path a cache
      # action can carry. So a hit still runs `install-deps`, and only the ~40 MB
      # Chromium download is skipped.
      #
      # Keyed on the lockfile: Playwright pins a browser revision per version, so
      # a bumped dependency must not replay the old binary against the new
      # client — that combination fails at launch with a version-mismatch error
      # that reads like a broken test.
      - uses: actions/cache@v4
        id: browsers
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}

      - if: steps.browsers.outputs.cache-hit == 'true'
        run: pnpm --filter ${{ matrix.e2e }} exec playwright install-deps chromium
      - if: steps.browsers.outputs.cache-hit != 'true'
        run: pnpm --filter ${{ matrix.e2e }} exec playwright install --with-deps chromium

      # The worker under test serves the built SPA through its `assets` binding,
      # so the directory has to exist before Playwright's webServer boots it.
      - uses: actions/cache/restore@v4
        with:
          path: .turbo/cache
          key: turbo-${{ runner.os }}-${{ github.sha }}
          restore-keys: turbo-${{ runner.os }}-

      - name: Build its SPA
        if: matrix.spa != ''
        run: pnpm turbo run build --filter=${{ matrix.spa }}

      - run: pnpm --filter ${{ matrix.e2e }} e2e

      # Traces + screenshots for the paths that failed. Nothing is attached on a
      # green run, so this stays cheap.
      - if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-report-${{ matrix.id }}
          path: |
            ${{ matrix.dir }}/playwright-report/
            ${{ matrix.dir }}/test-results/
          retention-days: 7
