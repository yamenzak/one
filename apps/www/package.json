{
  "name": "@mossa/www",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Mossa marketing site — a dependency-free static generator, deployed as a Cloudflare Worker with the static-assets binding.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && cd dist && python3 -m http.server 4321",
    "preview": "cd dist && python3 -m http.server 4321",
    "deploy": "node build.mjs && wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf .turbo dist"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.42.0"
  }
}
