# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.output/
.vite/
*.tsbuildinfo

# Turborepo
.turbo/

# Cloudflare / Wrangler
.wrangler/
.dev.vars
.dev.vars.*
# …but the template IS committed (it documents ENVIRONMENT=development).
!.dev.vars.example

# Env / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Test / coverage
coverage/
.nyc_output/
playwright-report/
test-results/

# The screenshot suite's output (`pnpm --filter @kova/e2e shots`). Regenerated
# on demand, so it is not history — the handful of images that actually ship
# (Help Center, marketing) are copied out by id and committed where they are
# used. See docs/help/README.md.
apps/e2e/shots-out/

# OS / editor
.DS_Store
Thumbs.db
.idea/
apps/scena-e2e/shots-out/
