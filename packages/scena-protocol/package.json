{
  "name": "@scena/protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Wire protocol between screens and Durable Objects: pairing, clock-sync handshake, manifest nudges, command + interrupt envelopes.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
