{
  "name": "@4dl/app-kit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The browser-side runtime every 4DL app repeats: the typed fetch layer with its offline/queued-write model, host resolution across the five doors, prefixed local storage, the passkey ceremony, Stripe.js + the inline payment sheet, Turnstile, the error boundary, and the deploy hard-refresh.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@4dl/tenancy": "workspace:*",
    "@4dl/ui": "workspace:*",
    "@simplewebauthn/browser": "^13.3.0"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@4dl/core": "workspace:*",
    "@types/react": "^19.2.14",
    "react": "^19.2.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
