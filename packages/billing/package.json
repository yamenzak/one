{
  "name": "@4dl/billing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Platform billing: the entitlement engine (quotas, feature gates, credit grants), credit metering, the per-tenant credit Durable Object, the Stripe client, and the dunning ladder. No product vocabulary \u2014 see README.md.",
  "exports": {
    ".": "./src/index.ts",
    "./model": "./src/model.ts",
    "./schema": "./src/schema.ts",
    "./stripe": "./src/stripe.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@4dl/core": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251001.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "@4dl/core": "workspace:*"
  }
}
