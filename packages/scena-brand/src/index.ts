/**
 * @scena/brand — the Scena mascot/logo as a pure SVG-string builder, plus its
 * palettes and scenario→mood mapping. Framework-agnostic so the vanilla player
 * and the React dashboard render the identical character.
 */
export {
  scenaMascotSvg,
  scenaIconSvg,
  type ScenaIconOptions,
  SCENA_MOODS,
  SCENA_PALETTES,
  DEFAULT_SCENA_PALETTE,
  SCENA_TOKEN_PALETTE,
  type ScenaPalette,
  type ScenaPose,
  type ScenaExpression,
  type ScenaMood,
  type ScenaMascotOptions,
} from "./mascot.js";
