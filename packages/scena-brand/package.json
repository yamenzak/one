{
  "name": "@scena/brand",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Scena brand \u2014 the mascot/logo as a pure, zero-dependency SVG builder. Renders in the vanilla player pairing screen and the React dashboard alike, tinted by palette or design tokens, expressive per scenario.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
