{
  "name": "@mossa/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure domain logic — credits, entitlements, permissions, access-economy budgets, nutrition/TDEE, body-fat, progress aggregates, workout math. No I/O.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
