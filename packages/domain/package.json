{
  "name": "@kova/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Kova's pure domain logic \u2014 nutrition/TDEE, body-fat, workout/activity math, progress, wellness, coaching, plus the product registries the platform substrate is parameterized by (entitlements, perms, budgets, notifications, features). No I/O. The shared multi-tenant half lives in @4dl/platform.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@4dl/auth": "workspace:*",
    "@4dl/billing": "workspace:*",
    "@4dl/tenancy": "workspace:*"
  }
}
