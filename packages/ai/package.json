{
  "name": "@4dl/ai",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The metered generation path: model catalog, Workers AI + Gemini providers, reserve\u2192run\u2192settle against the credit DO, the development-only mock lane, and provider pricing parsers. Feature prompts are the app's.",
  "exports": {
    ".": "./src/index.ts",
    "./model": "./src/model.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@4dl/billing": "workspace:*",
    "@4dl/core": "workspace:*"
  },
  "devDependencies": {
    "@4dl/core": "workspace:*",
    "@cloudflare/workers-types": "^4.20251001.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
