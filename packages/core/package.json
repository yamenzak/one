{
  "name": "@4dl/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The floor every 4DL package stands on: time-sortable ids, defensive JSON columns, the structural bindings contract, the composed schema-module runner, and the shared platform-config store. No product vocabulary, no I/O beyond D1 and the one config KV.",
  "exports": {
    ".": "./src/index.ts",
    "./boundary": "./src/boundary.ts",
    "./admin-routes": "./src/admin-routes.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "hono": "^4.9.10"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251001.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
