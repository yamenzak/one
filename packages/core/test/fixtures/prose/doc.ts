/**
 * A header that explains the boundary by naming what is on the other side of it:
 * a client, a workout, a meal plan, a studio. None of this is code.
 */
// A trailing comment about a coach and their trainer.
export const SAFE = 1; // and an inline aside about a supplement
